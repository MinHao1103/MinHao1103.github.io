> 🌱 **歷史存檔聲明**：
> 這是早期初學階段留下的學習筆記與基礎練習紀錄，保留於此作為自己一路走來的初心回顧。
> 若面試官或同行前輩想看進階架構與實戰踩坑，再麻煩直接參考上方「Java 底層與架構」或「Spring 生態系解析」等章節，謝謝！

---

# Java 基礎打底紀錄

目前這篇筆記主要留存早期學習 Java 的基礎語法與核心概念。
原因是打好基礎才能走得遠，這裡紀錄了當時對基本型別、控制結構與陣列的理解。

## 1. 變數與基本資料型別
- **基本型別 (Primitive Types)**：包含整數 (`int`, `long`)、浮點數 (`double`)、字元 (`char`) 與布林值 (`boolean`)。
- **變數生命週期**：區域變數必須給定初始值才能使用；實體變數則有預設值。

## 2. 流程控制與迴圈
- **條件判斷**：`if-else` 與 `switch-case`。
- **迴圈設計**：`for` (已知次數)、`while` (未知次數)。預計在使用無窮迴圈時，務必搭配 `break` 設立中斷條件。

## 3. 陣列與字串
- **陣列 (Array)**：宣告後長度固定。實務上現在多改用 `ArrayList` 來動態管理。
- **字串池 (String Pool)**：`String` 是不可變物件 (Immutable)。
  - 踩坑點：比較字串內容時，再麻煩一定要使用 `equals()`，絕對不要使用 `==` (這只會比較記憶體位址)。
