# Spring Cloud 微服務架構

Spring Cloud 提供了一套完整的微服務解決方案，以下是常用的核心組件：

## 1. 服務註冊與發現 (Eureka)
允許微服務在啟動時自動註冊到註冊中心，其他服務可以透過服務名稱來呼叫，而不需要硬編碼 IP 地址。

## 2. API 網關 (Gateway)
作為所有外部請求的單一入口，負責路由轉發、權限驗證 (JWT) 與限流。

## 3. 分散式配置中心 (Config Server)
集中管理所有微服務的配置檔（如 `application.yml`），並支援結合 Git 進行版本控制與熱更新。
